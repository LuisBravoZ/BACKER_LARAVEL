import './bootstrap';   // si estás usando Laravel's Bootstrap
import { createApp } from 'vue';  // Solo si estás usando Vue

// Crear una instancia de Vue, si estás usando Vue
const app = createApp({});
app.mount('#app');
